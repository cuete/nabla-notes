<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material3 dynamic color theme (uses system wallpaper colors on Android 12+) -->
    <style name="Theme.NablaNotes" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Transparent floating theme for TrackerPopupActivity -->
    <style name="Theme.NablaNotes.TrackerPopup" parent="Theme.NablaNotes">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
